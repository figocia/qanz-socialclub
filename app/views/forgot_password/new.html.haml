%section.forgot_password.container
  .row
    .col-sm-10.col-bg-offset-1
      = bootstrap_form_tag url: forgot_password_index_path, class: 'sign_in' do |f|

        %header
          %h1 Forgot Password?
          %p We will send you an email with a link that you can use to reset your password.
        .form-group
          .row
            .col-sm-4
              = f.email_field :email, label: 'Email Address'
        .form-group.action
          = f.submit 'Send email'
    